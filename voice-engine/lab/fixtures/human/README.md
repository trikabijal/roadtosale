# Voice Engine Lab — Human Audio Fixtures

Real human recordings (rep reading the scripts in `../scripts/`, room-noise variants, accent variants) will be dropped here once captured. Empty in v1 seed.
