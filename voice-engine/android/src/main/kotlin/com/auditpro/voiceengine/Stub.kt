/*
 * Stub.kt
 *
 * Placeholder for the Android native module that will host whichever
 * STT engine we settle on for Android (Argmax, Vosk, or a cloud
 * client). Not wired in v1.
 *
 * See:
 *   - voice-engine/docs/architecture.md (Strategy layer)
 *   - voice-engine/docs/api.md (TranscriptionStrategy contract)
 *
 * Build status: placeholder, no Gradle project around it yet.
 */

package com.auditpro.voiceengine

object Stub {
    const val PLACEHOLDER: String = "voice-engine android stub"
}
