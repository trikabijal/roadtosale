{
  "name": "road-to-sale-bff",
  "version": "1.0.0",
  "description": "Road to Sale BFF — Node/Fastify app-facing API; the only thing the mobile app talks to.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/multipart": "^9.0.1",
    "@fastify/swagger": "^9.2.0",
    "@fastify/swagger-ui": "^5.1.0",
    "fastify": "^5.1.0",
    "undici": "^7.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
