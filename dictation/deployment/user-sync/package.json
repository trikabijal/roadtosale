{
  "name": "justtalk-user-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mirror Keycloak users into the app_users table (Railway cron).",
  "engines": { "node": ">=20" },
  "scripts": { "start": "node sync.js" },
  "dependencies": { "pg": "^8.13.1" }
}
